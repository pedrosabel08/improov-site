# Content

Conteúdo versionado no repositório (sem CMS).

- `cases/`: dados de cada projeto
- `i18n/`: textos globais (menus, footer, labels)

A ideia é manter o conteúdo separado do código de UI (`src/`), para facilitar manutenção e revisão.
